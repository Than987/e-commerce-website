import React from 'react'
import '../App.css'
// import { useSelector } from 'react-redux'
// import { SelectorCreation } from '../redux/slices/CartSlice'

const Cart = () => {
  // const item = useSelector((state) => state,
  // (state)=>state.cart.cartItems)
  // const total = item.reduce((a, b) => a + b.price, 0)
  return (
    <>
      {/* <div className='Cart'>Total Items: {item.length}(Rs:{total})</div> */}

      <p>Header</p>
    </>
  )
}

export default Cart
